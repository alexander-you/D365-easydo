# Tests

> **תקציר בעברית:** תרחישי בדיקות קבלה (UAT) ומטריצת בדיקות. יושלם בהמשך.

| File | Purpose |
| --- | --- |
| `uat-scenarios.md` | User acceptance test scenarios (HE + EN) |
| `test-matrix.md` | Test matrix across statuses, languages and error paths |

These files will be populated as functionality is implemented.
